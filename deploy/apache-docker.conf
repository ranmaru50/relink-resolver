# deploy/apache-docker.conf
# Container 内の PHP エントリポイントと private data の境界。
Alias /relink /var/www/html
<Directory /var/www/html>
    AllowOverride All
    Require all granted
</Directory>
<Directory /var/lib/relink-resolver>
    Require all denied
</Directory>
