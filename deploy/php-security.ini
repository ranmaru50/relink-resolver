; deploy/php-security.ini
; PHP 実行環境の不要なバージョン情報を HTTP 応答から除外する。
expose_php = Off
; 管理面の本文・変数・処理時間・プロセス使用メモリを制限する。
post_max_size = 64K
max_input_vars = 32
arg_separator.input = "&"
max_input_time = 10
max_execution_time = 15
memory_limit = 128M
